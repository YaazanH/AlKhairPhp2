<!DOCTYPE html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Packages</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="bg-zinc-50"><main class="mx-auto max-w-5xl space-y-6 p-6 md:p-10"><a href="{{ route('platform.dashboard') }}" class="text-sm text-emerald-700">Back to tenant overview</a><header><p class="text-sm text-emerald-700">Platform administration</p><h1 class="text-3xl font-bold">Packages and features</h1><p class="mt-1 text-zinc-500">Choose the features included in each package. Core is always included because it contains the base application.</p></header>@if(session('status'))<div class="rounded-xl bg-emerald-50 p-4 text-emerald-800">{{ session('status') }}</div>@endif<div class="grid gap-6">@foreach($plans as $plan)<section class="rounded-2xl border bg-white p-6 shadow-sm"><form method="POST" action="{{ route('platform.plans.update',$plan) }}">@csrf @method('PUT')<div class="flex flex-col gap-4 md:flex-row md:items-start md:justify-between"><label class="grid gap-1 font-medium">Package name<input name="name" value="{{ $plan->name }}" class="rounded-xl border p-3 font-normal"></label><label class="inline-flex items-center gap-2 pt-7"><input type="checkbox" name="is_active" value="1" @checked($plan->is_active)> Active</label></div><fieldset class="mt-5"><legend class="font-medium">Included features</legend><div class="mt-3 grid gap-3 md:grid-cols-3">@foreach($features as $feature)<label class="rounded-xl border p-3 {{ $feature->is_core ? 'bg-zinc-50' : '' }}"><input type="checkbox" name="features[]" value="{{ $feature->code }}" @checked($feature->is_core || $plan->features->contains('id',$feature->id)) @disabled($feature->is_core)><span class="ml-2 font-medium">{{ $feature->name }}</span><span class="mt-1 block text-xs text-zinc-500">{{ $feature->is_core ? 'Always included' : ($feature->description ?: 'Optional feature') }}</span></label>@endforeach</div></fieldset><button class="mt-5 rounded-xl bg-emerald-700 px-4 py-3 text-white">Save package</button></form></section>@endforeach</div></main>@fluxScripts</body></html>
